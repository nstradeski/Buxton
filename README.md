# Buxton & Peak District Trip

Trip planning site, served via GitHub Pages.

## Local preview

```sh
python3 -m http.server 8000
# open http://localhost:8000
```

## Deploy

Push to `main`. In the GitHub repo settings → Pages, set source to `Deploy from a branch` → `main` / `(root)`.
